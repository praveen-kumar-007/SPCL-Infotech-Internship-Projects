# Backend .env example
PORT=5000
ADMIN_USER=admin
ADMIN_PASS=changeme
ADMIN_TOKEN=changeme
# Optional: where to read/write the stored JSON data (defaults to ./inquiries.json and ./applications.json)
INQUIRIES_FILE=./inquiries.json
APPLICATIONS_FILE=./applications.json
# Optional CORS origin - set to the frontend URL in production, or leave empty to allow all
# CORS_ORIGIN=https://your-frontend.example
